# The only application secret. Encryption and session-signing keys are derived from it.
# Any random string is accepted; no prefix is required.
# Generate your own with: openssl rand -base64 33
MASTER_KEY=replace-with-a-random-master-key

# Public application port. Compose binds it on all host interfaces (0.0.0.0).
ROUTER_PORT=8090

# Primary database: postgresql or clickhouse. Each mode is standalone.
DB_BACKEND=postgresql
DB_HOST=postgres
DB_PORT=5432
DB_USER=gorouter
DB_PASSWORD=change-me-database-password
DB_NAME=gorouter
# Internal Compose PostgreSQL does not provide TLS. Override for remote TLS databases.
DB_SSLMODE=disable

# Redis connection. Compose reaches it by service name on the internal network.
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_USER=gorouter
REDIS_PASSWORD=change-me-redis-password

# ClickHouse setup (used by docker-compose.clickhouse.yml)
CLICKHOUSE_PASSWORD=change-me-clickhouse-password
CLICKHOUSE_DB=gorouter
CLICKHOUSE_USER=gorouter
CLICKHOUSE_HOST=clickhouse
CLICKHOUSE_PORT=9000
CLICKHOUSE_TLS=false

# Optional application settings
LISTEN=:8090
APP_ENV=development
REQUEST_TIMEOUT=5m
CACHE_ENABLED=true
CACHE_TTL=24h
CACHE_SCOPE=key
CACHE_MAX_ENTRY_BYTES=1048576
CACHE_MAX_TOTAL_BYTES=268435456
CACHE_MEMORY_FALLBACK=true
REDIS_OUTAGE_POLICY=strict
API_TOKEN_CACHE_TTL=10m
WEEK_START=sunday
USAGE_WRITE_CONCURRENCY=4
USAGE_WRITE_QUEUE_SIZE=100000
REQUEST_LIMIT_MB=20
# Per-account upstream retry budget before routing moves to the next account.
# A single account runs to completion before the next one is tried; the request
# only fails once every eligible account is exhausted.
ROUTE_RETRIES=2
ANTHROPIC_OAUTH_CLIENT_ID=9d1c250a-e61b-44d9-88ed-5944d1962f5e
# Optional override for tests or compatible OAuth deployments.
ANTHROPIC_OAUTH_TOKEN_URL=
CODEX_OAUTH_CLIENT_ID=app_EMoamEEZ73f0CkXaXp7hrann
# Optional compatible/test override.
CODEX_OAUTH_TOKEN_URL=

# OpenRouter's public model/pricing catalog is loaded at startup and refreshed hourly.
OPENROUTER_CATALOG_ENABLED=true
OPENROUTER_CATALOG_URL=https://openrouter.ai/api/frontend/v1/catalog/models
OPENROUTER_SYNC_INTERVAL=1h
OPENROUTER_HTTP_TIMEOUT=30s

# Refresh provider-reported metadata for already imported model routes. New
# models remain an explicit import so provider changes cannot widen allowlists.
MODEL_CATALOG_SYNC_ENABLED=true
MODEL_CATALOG_SYNC_INTERVAL=15m
MODEL_CATALOG_CACHE_TTL=1h
# Public OAuth desktop/CLI client identifiers. Override only when using your
# own registered OAuth application.
GITHUB_OAUTH_CLIENT_ID=Iv1.b507a08c87ecfe98
GROK_OAUTH_CLIENT_ID=b1a00492-073a-47ea-816f-4c329264a828
KIMI_CODING_OAUTH_CLIENT_ID=17e5f671-d194-4dfb-9706-5516cb48c098
ANTIGRAVITY_OAUTH_CLIENT_ID=
ANTIGRAVITY_OAUTH_CLIENT_SECRET=
